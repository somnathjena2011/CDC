from .arithmetic import *

print('hello world')

#uses of __init__.py:
#-> makes the directory in which it is present a python package(before 3.3)
#-> can import inside init file so that no need to import modules in main
#-> used to instantiate login configurations